import Button from '@/components/ui/Button'
import React from 'react'

interface pageProps{

}

const page: React.FC<pageProps> = ({}) => {
  return (
    <Button>
        Hello
    </Button>
  )
}

export default page